compare mode
dark mode feature need to work

bugs:
redirect to exact path if user logged in
start time should be same for payment modal
need to work delete coupon
admin can't retun future date on rental cost modal (calculator modal)
homepage search button need to work